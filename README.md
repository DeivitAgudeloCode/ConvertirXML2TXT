# ConvertirXML2TXT
Programa especialmente creado y especifico para un empresa que solicito convertir un XML puro a un archivo descargable en formato de texto, la sintaxis puede ser usada para validar converciones.

Lenguaje usado: 
C# / XML(Para el mapeo de los datos).
